@model IEnumerable<Escola.Models.MateriaLecionada>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Professor.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ano)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Semestre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MateriaID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Professor.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ano)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Semestre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MateriaID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MateriaLecionadaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MateriaLecionadaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MateriaLecionadaID })
        </td>
    </tr>
}

</table>
